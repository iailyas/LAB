sudo apt update
sudo apt install gnupg

sudo apt install gnupg

gpg --gen-key
gpg --list-public-keys
gpg --list-secret-keys


gpg --export -a ilya kuzmenkow>public.key
cat public.key


gpg --export-secret-key -a ilya kuzmenkow>secret.key
cat secret.key

gpg --export --armor -a ilya kuzmenkow>public.key
gpg --export-secret-key --armor -a ilya kuzmenkow>secret.key

cat public.key
cat secret.key

gpg --export-secret-keys ilya kuzmenkow >secret.gpg
cat secret.gpg

touch 1.txt 
entered 'строку EncryptMe AUE Please' 
gpg -e --armor 1.txt
entered specified data => user ID ilya kuzmenkow

gpg 1.txt.gpg *and entered phase*
cat 1.txt

gpg --delete-secret-key ilya
gpg --list-secret-key ilya

gpg --delete-key ilya
gpg --list-key ilya

gpg --import secret.key
gpg -d 1.txt.gpg
