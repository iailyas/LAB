sudo useradd John 
sudp useradd Raymond

sudo usermod -aG users John
sudo usermod -aG users Raymond

su John 
su Raymond

sudo userdel 
exit

groupadd users

sed- stream editor for filtering and transfprming text

sed 's/:.*//' /etc/passwd - shows all users
lastlog

less /etc/group

passwd John
passwd Raymond

sudo apt-get install openssh-server
sudo systemctl enable ssh
sudo systemctl restart ssh.service

nano /etc/ssh/sshd_config
ssh John@127.0.0.1 - using password

su Raymond 
ssh-keygen

sudo apt-get install ssh


Match User John,Ilya
  PasswordAuthentication yes

Match User Raymond
  PasswordAuthentication no

sudo systemctl restart ssh
sudo systemctl restart ssh.service
systemctl status ssh

ssh-keygen

chown -R Raymond: /home/Raymond/.ssh

ssh -i ~/.ssh/id_rsa - with passwordauthentication
ssh -i ~/.ssh/id_rsa Raymond@127.0.0.1

sudo cp /etc/sudoers /etc/sudoers.bak

/etc/sudoers 
Cmnd_Alias DISABLE_SU = /bin/su
John ALL=(ALL) NOPASSWD: ALL, !DISABLE_SU


usermod -aG sudo Raymond - to give opportunity use SUDO

chmod 754 1.txt Raymond

#!/bin/bash
echo "Hello World"

bash First.sh


chmod 754 First.sh

usermod -s /bin/bash John
sudo useradd -g users -s /bin/sh givi


add/watch result 
sudo useradd -g users -s /bin/sh givi
root@ilya-VirtualBox:/home/John# su givi
$ cd /home/John/
$ ls
First.sh
$ bash First.sh
Hello World

scp First.sh ilya@127.0.0.1:/home/ilya
scp ilya@127.0.0.1:/home/ilya/First.sh /home/John

